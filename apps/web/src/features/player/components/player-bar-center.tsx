import { Box } from '@/features/ui/box/box.tsx';
import styles from './player-bar-center.module.scss';

export const PlayerBarCenter = () => {
    return <Box className={styles.center}>Center</Box>;
};
